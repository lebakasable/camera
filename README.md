# camera

```console
# Base de données
$ sudo apt install postgresql
$ sudo service postgresql restart
# Langage Elixir
$ sudo apt install elixir
# Gestionnaire de packet d'Elixir
$ mix local.hex
$ mix archive.install hex phx_new
$ mix phx.setup
# Création base de données
$ mix ecto.create
# Lancement serveur
$ mix phx.server
```
